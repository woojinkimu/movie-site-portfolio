import React from 'react';
import styles from './board_form_button.module.css';

const BoardFormButton = (props) => {
  return (
    <button className={styles.button}>작성</button>
  );
};

export default BoardFormButton;